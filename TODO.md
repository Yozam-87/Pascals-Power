## 🟡 Medium

**1.** Strip leading timestamp prefix from `llama-server` log output.

**2.** ~~Expand the custom flag name autocomplete.~~ ✅ Done — 91 flags parsed from `llama-server -h` and populated via JS array.

## 🟢 Low

**3.** Remove the MoE regex display from the layer split calculator section. The copy button already provides the generated command, so the raw regex is redundant and cluttering the UI.

**4.** Expand the sampler parameter specification. More sampler options should be exposed in the GUI, with specific flags to be determined.